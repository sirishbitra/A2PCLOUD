doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
    <!-- Global site tag (gtag.js) - Google Analytics -->
    script(async=true, src="https://www.googletagmanager.com/gtag/js?id=UA-93490642-1")
    each item in metaData
      meta(property=item.metaType content=item.metaValue)
  body
    a(href=url) click here if not redirected.
    script var redirectedUrl = '#{url}';
    script
      include ./script.js
